import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `"Abhaya Libre", sans-serif`,
    body: `"Abhaya Libre", sans-serif`,
  },
});

export default theme;
